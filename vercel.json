{
  "buildCommand": "cp prisma/schema.postgres.prisma prisma/schema.prisma && npx prisma generate && next build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/sync-scores",
      "schedule": "0 2 * * *"
    },
    {
      "path": "/api/sync-scorers",
      "schedule": "0 3 * * *"
    }
  ]
}
