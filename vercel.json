{
  "buildCommand": "cp prisma/schema.postgres.prisma prisma/schema.prisma && npx prisma generate && npx prisma db push && next build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/sync-scores",
      "schedule": "*/5 * * * *"
    }
  ]
}
